import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { API_BASE_URL } from '../config';

const EventerSettings = () => {
  return (
    <div>
      <h1>イベンター用のアカウント設定画面</h1>
    </div>
  );
};

export default EventerSettings;
